{
  "name": "groundstation",
  "version": "0.1.0",
  "description": "Mission control for a small cloud fleet — a terminal command center over gcloud + Ansible.",
  "type": "module",
  "bin": {
    "gnd": "./src/index.tsx"
  },
  "scripts": {
    "start": "bun run src/index.tsx",
    "dev": "DEV=true bun run src/index.tsx",
    "serve": "bun run src/index.tsx serve",
    "typecheck": "tsc --noEmit",
    "compile": "bun build --compile ./src/index.tsx --minify --outfile gnd",
    "install-local": "bun run compile && mkdir -p ~/.local/bin && cp gnd ~/.local/bin/gnd",
    "test": "bun test"
  },
  "dependencies": {
    "@opentui/core": "^0.4.5",
    "@opentui/keymap": "^0.4.5",
    "@opentui/react": "^0.4.5",
    "@opentui/ssh": "^0.4.5",
    "fuzzysort": "^3.1.0",
    "opentui-spinner": "^0.0.7",
    "react": "^19.2.0"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "@types/react": "^19.0.0",
    "typescript": "^5.6.0"
  }
}
